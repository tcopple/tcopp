body > footer {
  @include base_dark;
  @extend .sans;

  font-size: .8em;
  text-align: right;
  width: 100%;
  height: auto;
  float: left;

  margin: 0px auto;
  p:last-child { margin-bottom: 0; }
}
